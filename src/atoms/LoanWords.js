import { atom } from 'recoil';

export const inputLoanValue = atom({
  key: 'inputLoanValue',
  default: '',
});

/*export const loanWordValue = atom({
  key: 'loanWordValue',
  default: '',
});*/

export const loanMeaningValue = atom({
  key: 'loanMeaningValue',
  default: '',
});

export const exampleLoanValue = atom({
  key: 'exampleLoanValue',
  default: '',
});

export const exampleKoreanValue = atom({
  key: 'exampleKoreanValue',
  default: '',
});
